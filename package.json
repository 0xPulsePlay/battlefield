{
  "name": "battlefield",
  "version": "0.1.0",
  "private": true,
  "description": "The Probability Battlefield — a World Cup match rendered as a war between two armies, driven by de-margined win probabilities and possession-danger data (TxLINE). UI prototype phase: synthetic driver, real data bridge to follow.",
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 4400 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 4400 --strictPort",
    "test": "node --test bridge/"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@txline/client-sdk": "file:../.nightshift/vendor/txline-client-sdk-0.1.0.tgz",
    "@txline/verify": "file:../.nightshift/vendor/txline-verify-0.1.0.tgz"
  }
}
